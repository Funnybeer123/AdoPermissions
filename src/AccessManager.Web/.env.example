# Sandbox organization. Only a simple slug is accepted.
AZURE_DEVOPS_ORG=evanbeer

# Short-lived, org-scoped, read-only PAT. Never commit the value.
# Create it in Azure DevOps user settings and store it only as an
# environment secret. The leaked chat token must not be reused.
AZURE_DEVOPS_PAT=
